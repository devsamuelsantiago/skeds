{
  "name": "skeds",
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test",
    "dev": "lerna run dev",
    "clean": "yarn dlx rimraf node_modules */**/node_modules",
    "reset": "yarn clean && yarn"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.2",
    "prettier": "^3.2.5"
  },
  "packageManager": "yarn@4.1.1"
}
